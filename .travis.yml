language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t myrkotyn/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true myrkotyn/docker-react npm run test -- --coverage
